/* eslint-disable react-refresh/only-export-components */
import React from "react";
import ListRecords from "../../components/list-records/ListRecords";

const ListRecordPage = () => (
  <div>
    <ListRecords />
  </div>
);

export default React.memo(ListRecordPage);
